import React from 'react';

const Header = () => {
    return (
        <React.Fragment>
            <header>
                header
            </header>
        </React.Fragment>
    );
};

export default Header;